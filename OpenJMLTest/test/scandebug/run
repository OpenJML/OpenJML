#! /bin/bash

if [ -z "$OJA" ]; then OJA=openjml ; fi
OJ=scanner $OJA --check A.java > actual
if [ $? != 0 ]; then echo Failed; exit 1; fi
num=`grep "TOKEN " actual | wc -l`
if [ $num = 0 ]; then echo No tokens; exit 1; fi
exit 0
