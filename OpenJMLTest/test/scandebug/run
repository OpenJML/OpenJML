#! /usr/bin/env bash

LOCAL=$(dirname "$BASH_SOURCE[0]" )
. "$LOCAL/../setup-script"
 
OJ=scanner openjml --check A.java > actual
if [ $? != 1 ]; then echo Failed ; exit 1; fi
num=`grep "TOKEN " actual | wc -l`
if [ $num -eq 0 ]; then echo No tokens; exit 1; fi
exit 0
