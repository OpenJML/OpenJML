#! /usr/bin/env bash

LOCAL=$(dirname "$BASH_SOURCE[0]" )
. "$LOCAL/../setup-script"

. ./runjava.sh &> actual
(diff actual expected  && rm -f actual) || exit 1
. ./run.sh &> actual

diff actual expected && rm -f actual
exit $?
