public class Loops {
/*
  public void m1() {
    int i = 10;
    //@ loop_invariant i == \count;
    for (i = 0; i < 5; i++) {
      i += 20;
      //@ assert \old(i,\LoopInit) == 10;
      //@ assert \old(i, \LoopBody) == \count;
      i -= 20;
    }
  }

  public void m2() {
    //@ loop_invariant i == \count;
    for (int i = 0; i < 5; i++) {
      i += 20;
      //@ assert \old(i,\LoopInit) == 10;
      //@ assert \old(i, \LoopBody) == \count;
      i -= 20;
    }
  }
*/
  public void m3() {
    int i = 0;
    //@ loop_invariant i == \count;
    while (i<5) {
      i += 20;
      //@ assert \old(i,\LoopInit) == 10;
      //@ assert \old(i, \LoopBody) == \count;
      i -= 20;
    }
  }
/*
  public void m4() {
    int i = 0;
    //@ loop_invariant i == \count;
    do {
      i += 20;
      // @ assert \old(i,\LoopInit) == 10;
      // @ assert \old(i, \LoopBody) == \count;
      i -= 20;
    } while (i<5);
  }

  public void m5(int[] a) {
    int j = 123;
    for (int k: a) {
      k += 20;
      j = 0;
      // @ assert \old(k, \LoopBody) == a[\count];
      // @ assert \old(j, \LoopInit) == 13;
    }
  }
*/
}
