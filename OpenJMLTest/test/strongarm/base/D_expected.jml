
import org.jmlspecs.annotation.*;

public class D {
  
  public D();
    /*@
    public normal_behavior
      requires true; 
      {|
        requires (a < 10); 
        ensures \result == 100; 
      also
        requires !(a < 10); 
        ensures \result == a; 
      |}
   */

  @Pure 
  public static int pathing(int a);
}