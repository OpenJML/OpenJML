#! /usr/bin/env bash

cd $(dirname "$BASH_SOURCE[0]" )
BIN="../../../OpenJML21"
if [ -z "$OJA" ]; then OJA=$BIN/openjml; fi

($BIN/openjml-compile --compile Run.java && $BIN/openjml-run Run) |& diff - RUN.out
if [ $? -ne 0 ]; then echo test/apiinstance FAILED ; fi
