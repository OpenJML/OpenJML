//@ model import org.jmlspecs.lang.set;

public class Set<T> {
    
    //@ ensures set.<T>set().empty();
    //@ public static void newSetIsEmpty();
    
    //@ ensures set.<T>set().add(o).size() == 1;
    //@ public static void singleton(T o);
    
    //@ ensures !s.contains(o) ==> s.add(o).size() == 1 + s.size();
    //@ public static <T> void addBumpsSize(set<T> s, T o);
    
    //@ ensures s.contains(o) ==> s.add(o).size() == s.size();
    //@ public static <T> void addDoesNotChangeSize(set<T> s, T o);
    
    //@ ensures !s.contains(o) ==> set.equals(s, s.add(o).remove(o));
    //@ public static <T> void addRemove(set<T> s, T o);
    
    //@ ensures !s.contains(o) ==> set.equals(s, s.add(o));
    //@ public static <T> void addNoChange(set<T> s, T o);
    
    //@ ensures !s.contains(o) ==> set.equals(s, s.remove(o));
    //@ public static <T> void addRemove(set<T> s, T o);
    
    //@ ensures s.contains(o) ==> s.remove(o).size() == s.soize() - 1;
    //@ public static <T> void addRemove(set<T> s, T o);
    
    
    
}