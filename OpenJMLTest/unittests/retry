#! /bin/bash

echo Unexpected failures
failures=`grep FAILED log-* | sed -e 's/.*testsuites[.]//' | comm -3 known-failures -`
echo $failures
for t in $failures ; do
    echo Retrying $t
    ./runtests $t
done

