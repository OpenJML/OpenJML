/** Companion spec file for A.java.
 *  This file REPLACES method specs and invariants written directly in A.java.
 *
 *  Scenario 3: aField is re-declared as a spec stub — Find Declaration should
 *              still navigate to A.java (the .java entry takes precedence).
 *  Scenario 4: ghostInAJml is declared ONLY here — Find Declaration navigates here. */
public class A {
    //@ ghost public int ghostInAJml = 0; // (4) ghost declaration only in A.jml
    public int aField;                   // (3) spec stub — declaration is in A.java

    // Class-level invariants: all four symbols used (public visibility to match public fields)
    //@ public invariant aField >= 0;           // (3) aField in A.jml invariant
    //@ public invariant ghostInAJml >= 0;      // (4) ghostInAJml in A.jml invariant
    //@ public invariant cObj.cField >= 0;      // (1) cField in A.jml invariant
    //@ public invariant cObj.cGhostField >= 0; // (2) cGhostField in A.jml invariant

    // Method spec for aMethod2: all four symbols in requires clauses
    //@ requires aField >= 0;            // (3) aField in A.jml method spec
    //@ requires ghostInAJml >= 0;       // (4) ghostInAJml in A.jml method spec
    //@ requires cObj.cField >= 0;       // (1) cField in A.jml method spec
    //@ requires cObj.cGhostField >= 0;  // (2) cGhostField in A.jml method spec
    public int aMethod2();
}
