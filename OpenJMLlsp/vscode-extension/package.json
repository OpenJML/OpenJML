{
  "name": "openjml",
  "displayName": "OpenJML",
  "description": "JML specification type-checking and static verification for Java via OpenJML",
  "version": "0.0.1",
  "publisher": "openjml",
  "engines": { "vscode": "^1.75.0" },
  "categories": ["Linters"],
  "activationEvents": ["onLanguage:java"],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "openjml.runEsc",
        "title": "Run ESC",
        "category": "OpenJML"
      },
      {
        "command": "openjml.saveAndRunEsc",
        "title": "Save and Run ESC",
        "category": "OpenJML"
      }
    ],
    "keybindings": [
      {
        "command": "openjml.saveAndRunEsc",
        "key": "ctrl+k ctrl+e",
        "mac": "cmd+k cmd+e",
        "when": "editorLangId == java && editorFocus"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "openjml.runEsc",
          "when": "resourceLangId == java",
          "group": "1_openjml"
        }
      ],
      "commandPalette": [
        {
          "command": "openjml.runEsc",
          "when": "resourceLangId == java"
        },
        {
          "command": "openjml.saveAndRunEsc",
          "when": "resourceLangId == java"
        }
      ]
    },
    "configuration": {
      "title": "OpenJML",
      "properties": {
        "openjml.serverPath": {
          "type": "string",
          "default": "",
          "order": 0,
          "description": "Path to the openjml-lsp launcher script. Leave empty to use the script bundled with this extension (development) or the one found via the extension's default location."
        },
        "openjml.checkTriggerOn": {
          "type": "string",
          "enum": ["edit", "save"],
          "default": "edit",
          "order": 1,
          "markdownDescription": "When to run the JML type-check (`--check`):\n- `edit` — check on every change (instant feedback)\n- `save` — check only when the file is saved"
        },
        "openjml.escTriggerOn": {
          "type": "string",
          "enum": ["manual", "save", "edit"],
          "default": "manual",
          "order": 2,
          "markdownDescription": "When to run extended static checking (`--esc`):\n- `manual` — only via the **OpenJML: Run ESC** command or editor button\n- `save` — on every save\n- `edit` — on every change (expensive; not recommended for large files)"
        },
        "openjml.specsPath": {
          "type": "string",
          "default": "",
          "order": 3,
          "description": "Path to the OpenJML specs directory. Leave empty to use the default from the launcher script."
        },
        "openjml.solversPath": {
          "type": "string",
          "default": "",
          "order": 4,
          "description": "Path to the SMT solvers directory. Leave empty to use the default from the launcher script."
        },
        "openjml.sourcePath": {
          "type": "string",
          "default": "",
          "order": 5,
          "description": "Source root(s) for resolving cross-file references (-sourcepath). Separate multiple roots with ':' (Unix) or ';' (Windows). Leave empty for single-file projects."
        },
        "openjml.classPath": {
          "type": "string",
          "default": "",
          "order": 6,
          "description": "Classpath for pre-compiled dependencies (-classpath). Separate multiple entries with ':' (Unix) or ';' (Windows). Leave empty if no external jars are needed."
        },
        "openjml.warnEscOnDirtyFile": {
          "type": "boolean",
          "default": true,
          "order": 7,
          "description": "Warn when 'OpenJML: Run ESC' is invoked on a file with unsaved changes. ESC runs on the saved file on disk, so results may not match the editor contents."
        }
      }
    }
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
